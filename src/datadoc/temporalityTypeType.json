{
    "$schema": "http://json-schema.org/draft-07/schema#",

    "temporalityTypeType": {
        "$comment": "Reusable temporalitytype type",
        "type": "string",
        "enum": [
            "FIXED",
            "STATUS",
            "ACCUMULATED",
            "EVENT"
        ]
    }
}